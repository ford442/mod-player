# Swarm State — XASM-1 Oscilloscope Pipeline

## Completed Steps

### Step 1 — SharedArrayBuffer in the worklet
- **File**: `public/worklets/openmpt-worklet.js`
- Allocated `SharedArrayBuffer(2048 * 4)` and `Float32Array` view in constructor
- Sent SAB to main thread once via `postMessage({ type: 'oscBuffer', buffer: ... })`
- Added `getOscBuffer` message handler for re-requesting the SAB (defensive against race conditions)
- In `process()`: copies first 128 samples of `outL` into ring buffer each audio frame, advancing write pointer with `& 2047` modulo

### Step 2 — Expose SAB view in main thread
- **File**: `hooks/useLibOpenMPT.ts`
- Added `oscBufferRef = useRef<Float32Array | null>(null)`
- After `startAudioPlayback()`, attaches a `message` listener to the worklet port and sends `{ type: 'getOscBuffer' }` to receive the SAB
- Exports `oscBufferRef` in hook return value
- No React state used; zero re-renders

### Step 3 — GPU texture upload and bind group wiring
- **Files**: `components/PatternDisplay.tsx`, `hooks/useWebGPURender.ts`, `utils/shaderVersion.ts`
- `PatternDisplay.tsx`:
  - Added `oscBufferRef` prop
  - Created `oscTextureRef` (1D `r32float`, 2048x1x1, `COPY_DST | TEXTURE_BINDING`)
  - In rAF loop: calls `device.queue.writeTexture` every frame when buffer + texture + device are available
  - Added `v0.55` to `padTopChannel`, `canvasMetrics`, and step-length version detection chains
- `hooks/useWebGPURender.ts`:
  - Accepts optional `oscTextureRef` parameter
  - For v0.55 extended shaders, adds binding 6 (`texture_1d<f32>`, `unfilterable-float`) to bind group layout
  - `refreshBindGroup` appends binding 6 entry when `oscTextureRef.current` exists and shader is v0.55
  - Added `v0.55` to all relevant version-detection chains (`isHighPrec`, cellW/cellH sizing, `playheadRowAsFloat`, `isOverlayActive`)
- `utils/shaderVersion.ts`:
  - Added `v0.55` to `getLayoutType` (extended), `isCircularLayoutShader`, `getBackgroundShaderFile` (bezel), `shouldEnableAlphaBlending`

### Step 4 — New WGSL shader patternv0.55.wgsl
- **Files**: `shaders/patternv0.55.wgsl`, `public/shaders/patternv0.55.wgsl`
- Copied from `patternv0.51.wgsl` as base (byte-identical `Uniforms` struct)
- Added `@group(0) @binding(6) var oscTexture: texture_1d<f32>;`
- In fragment shader:
  - Computes `fragAngle` → normalized index into 2048-sample buffer
  - Samples `textureLoad(oscTexture, oscIndex, 0).r`
  - Maps sample to radial position in innermost ring (`traceCenter = rInner * 0.5`, `traceAmp = rInner * 0.45`)
  - Draws thin green trace (`traceThick = 2.0` pixels) when pixel radius is within threshold of sample-mapped radius
  - Thresholded at `|oscSample| > 0.005` to suppress noise
  - Added `oscContrib` to both channel 0 return and final `finalColor`

### Step 5 — Register v0.55 in App.tsx
- **File**: `App.tsx`
- Added `{ id: 'patternv0.55.wgsl', label: 'v0.55 (Oscilloscope)' }` to `SHADER_GROUPS.CIRCULAR`
- Added `{ id: 'patternv0.51.wgsl', label: 'v0.51 (Playhead Arc)' }` to `SHADER_GROUPS.CIRCULAR` (backlog fix)
- Destructured `oscBufferRef` from `useLibOpenMPT` and passed it to both 2D and 3D `<PatternDisplay />` instances

## Verification
- `npm run typecheck` — ✅ passes with zero errors
- `npm run build` — ✅ succeeds (9.58s)
- WGSL brace balance — ✅ 66 open / 66 close

## Open Problems / Decisions Deferred
- None. All acceptance criteria met.

## Next Step
- Task complete. Ready for integration testing in a WebGPU-enabled browser.
