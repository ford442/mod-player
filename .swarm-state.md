## Step 2 – Implementation Complete
Completed:
1. Updated `types.ts` with `_openmpt_module_get_duration_seconds`, `WorkerParseRequest`, `WorkerParseResponse`, `WorkerParseError`, and related types.
2. Created `workers/openmpt-parser.worker.ts`:
   - Loads libopenmpt from CDN via `fetch` + `new Function()` (mirrors existing AudioWorklet WASM bootstrap pattern).
   - Calls `_openmpt_module_create_from_memory2()`, scans patterns with `getPatternMatrix()`, extracts metadata.
   - Destroys the worker-side module after parsing (one-shot).
   - Posts `{ patternMatrices, metadata }` back to main thread.
3. Refactored `hooks/useLibOpenMPT.ts`:
   - `processModuleData` now sends the `ArrayBuffer` to the worker (transferred, not copied) and awaits results.
   - Main-thread module creation removed from the normal load path.
   - Added `ensureMainThreadModule()` callback for lazy ScriptProcessor fallback module creation.
   - `seekToStepWrapper` refactored to derive order/row counts from `patternMatricesRef` instead of the main-thread module.
   - Cleanup effect now terminates the worker on unmount.
4. Updated `hooks/useAudioGraph.ts`:
   - Added `ensureMainThreadModuleRef` to `AudioGraphRefs`.
   - ScriptProcessor fallback path now lazily creates the main-thread module if it doesn't exist.
   - Made `node.port.onmessage` handler `async` to support the `await` in the fallback path.

Build & typecheck:
- `npm run typecheck` passes with zero errors.
- `npm run build` succeeds; Vite correctly emits `dist/assets/openmpt-parser.worker-*.js` (3.05 kB).
- No TODOs/FIXMEs/XXXs remain in the worker file.

Next: User review / verification. The parse logic is now fully off the main thread.
Blockers: None.
