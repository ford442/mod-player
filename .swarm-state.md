## DURA-001 GPU Compute Port — Implementation Log

### 2026-05-30 04:50+02:00 — Plan written
- Read `utils/gpuPacking.ts`, `hooks/useWebGPURender.ts`, `components/PatternDisplay.tsx`, `types.ts`
- Analyzed `calculateNoteDurations` + `packPatternMatrixHighPrecision` logic
- Designed compute shader that reads raw-packed 2×u32 input and writes high-precision 2×u32 output
- Identified integration point in `useWebGPURender.ts` cells-buffer creation

### 2026-05-30 05:15+02:00 — Compute shader + pipeline + gpuPacking changes
**Files touched:**
- `shaders/compute_note_duration.wgsl` (new)
- `public/shaders/compute_note_duration.wgsl` (copy)
- `utils/computeNoteDuration.ts` (new)
- `utils/gpuPacking.ts` — added `packPatternMatrixComputeInput` and `verifyDurationParity`
- `hooks/useWebGPURender.ts` — integrated compute path with fallback, dev parity check

**Key decisions:**
- `packPatternMatrixComputeInput` extracts cell fields identically to `packPatternMatrixHighPrecision` but packs into standard `[note][inst][volCmd][volVal]` / `[unused:16][effCmd][effVal]` format — this is the compute input.
- Compute shader does per-channel forward scan (1 thread per channel, `@workgroup_size(1,1,1)`), sequential row loops. Max rows = 1024 (IT format limit).
- Duration clamping to 255 rows replicated in all 3 scan branches (note-on end, note-off end, end-of-pattern tail).
- DURA-003 note copying, expression-only flag, volume nibble compression all replicated.
- Parity check runs async in dev mode: readback compute output → compare byte-for-byte with `packPatternMatrixHighPrecision` CPU output.

**Status:**
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, 16.5s)

### 2026-05-30 05:30+02:00 — Committed
Commit `a4d8978` with 6 files changed, 800 insertions, 35 deletions.

### Next steps / blockers
- **Browser smoke-test required** with at least one MOD and one IT module to verify the dev-mode parity assertion `[DURA-PARITY] ✓` prints in the console.
- If parity fails, the assertion logs the exact cell index and hex values of the first mismatch for debugging.
- No push/PR per instructions — Noah/Jules handle integration.

**Blockers:** None (implementation complete; pending runtime verification).

---

## MOBILE-LITE Render Mode — Implementation Log

### 2026-06-05 23:45+02:00 — Design & first pass
**Objective:** Implement a parallel "lite" render branch for mobile / low-power GPUs. Desktop path must remain byte-identical.

**Files touched:**
- `utils/deviceCapabilities.ts` (new)
- `utils/geometryConstants.ts`
- `utils/shaderRegistry.ts`
- `utils/shaderVersion.ts`
- `hooks/useWebGPURender.ts`
- `components/PatternDisplay.tsx`
- `App.tsx`

**Key decisions:**
- Capability detection is conservative: mobile UA string, missing WebGPU, and optional async low-power GPU adapter hint. URL param `?lite=1|0` and localStorage `xasm1_lite_mode` override auto-detection for desktop testing.
- Centralized policy object `DEVICE_CAPABILITIES` exported from `deviceCapabilities.ts`; consumers read flags, they don't re-detect.
- `patternv0.21.wgsl` tagged as `liteRecommended` in `shaderRegistry.ts` — horizontal layout, single-pass composite, no alpha blending, no layered bloom, supports 32-step paging.
- Lite mode auto-substitutes the display shader via `displayShaderFile` in `App.tsx` without mutating the user's stored desktop preference. Toggling lite off restores the previous shader instantly.
- Bloom initialization is skipped entirely in lite mode (`PatternDisplay.tsx` effect returns early when `liteMode` is true).
- WebGL overlay is disabled in lite mode (`isOverlayActive = !liteMode && WEBGL_HYBRID_SHADERS.has(...)`), saving an extra context.
- Canvas metrics reduced to 512×512 in lite mode (half the desktop 1024×1024), cutting fill-rate and memory.
- Compute path gated by `liteMode` in `useWebGPURender.ts` — falls back to CPU packing on the unlikely event a high-precision shader is forced in lite mode.
- DPR capped to 1.0 in lite mode (`PatternDisplay.tsx` `syncCanvasSize`), cutting physical canvas buffer size by up to 4× on high-DPR mobile screens.
- Desktop path is untouched: when `liteMode` is false, every code branch is identical to pre-change behavior (verified by conditional checks).

**Status:**
- `npm install` ✅ (missing `@tanstack/react-query` and `zod` installed)
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, 18.3s)

### Remaining / blockers
- **Runtime browser verification pending.** Need to confirm:
  1. `?lite=1` forces lite mode, substitutes v0.21, skips bloom, disables overlay, renders at 512×512.
  2. `?lite=0` forces full desktop mode even on mobile UA.
  3. Desktop default (`?lite` absent, localStorage empty, desktop UA) renders exactly as before (same shader, same bloom, same canvas size).
  4. No WebGPU console errors in either path.
- If runtime testing reveals any pipeline/buffer mismatch, the fix will be in `PatternDisplay.tsx` or `useWebGPURender.ts` (no structural changes expected).

**Blockers:** None (implementation complete; pending runtime verification).
