## DURA-001 GPU Compute Port — Implementation Log

### 2026-05-30 04:50+02:00 — Plan written
- Read `utils/gpuPacking.ts`, `hooks/useWebGPURender.ts`, `components/PatternDisplay.tsx`, `types.ts`
- Analyzed `calculateNoteDurations` + `packPatternMatrixHighPrecision` logic
- Designed compute shader that reads raw-packed 2×u32 input and writes high-precision 2×u32 output
- Identified integration point in `useWebGPURender.ts` cells-buffer creation

### 2026-05-30 05:15+02:00 — Compute shader + pipeline + gpuPacking changes
**Files touched:**
- `shaders/compute_note_duration.wgsl` (new)
- `public/shaders/compute_note_duration.wgsl` (copy)
- `utils/computeNoteDuration.ts` (new)
- `utils/gpuPacking.ts` — added `packPatternMatrixComputeInput` and `verifyDurationParity`
- `hooks/useWebGPURender.ts` — integrated compute path with fallback, dev parity check

**Key decisions:**
- `packPatternMatrixComputeInput` extracts cell fields identically to `packPatternMatrixHighPrecision` but packs into standard `[note][inst][volCmd][volVal]` / `[unused:16][effCmd][effVal]` format — this is the compute input.
- Compute shader does per-channel forward scan (1 thread per channel, `@workgroup_size(1,1,1)`), sequential row loops. Max rows = 1024 (IT format limit).
- Duration clamping to 255 rows replicated in all 3 scan branches (note-on end, note-off end, end-of-pattern tail).
- DURA-003 note copying, expression-only flag, volume nibble compression all replicated.
- Parity check runs async in dev mode: readback compute output → compare byte-for-byte with `packPatternMatrixHighPrecision` CPU output.

**Status:**
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, 16.5s)

### 2026-05-30 05:30+02:00 — Committed
Commit `a4d8978` with 6 files changed, 800 insertions, 35 deletions.

### Next steps / blockers
- **Browser smoke-test required** with at least one MOD and one IT module to verify the dev-mode parity assertion `[DURA-PARITY] ✓` prints in the console.
- If parity fails, the assertion logs the exact cell index and hex values of the first mismatch for debugging.
- No push/PR per instructions — Noah/Jules handle integration.

**Blockers:** None (implementation complete; pending runtime verification).
