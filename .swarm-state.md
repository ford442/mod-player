## DURA-001 GPU Compute Port — Implementation Log

### 2026-05-30 04:50+02:00 — Plan written
- Read `utils/gpuPacking.ts`, `hooks/useWebGPURender.ts`, `components/PatternDisplay.tsx`, `types.ts`
- Analyzed `calculateNoteDurations` + `packPatternMatrixHighPrecision` logic
- Designed compute shader that reads raw-packed 2×u32 input and writes high-precision 2×u32 output
- Identified integration point in `useWebGPURender.ts` cells-buffer creation

### 2026-05-30 05:15+02:00 — Compute shader + pipeline + gpuPacking changes
**Files touched:**
- `shaders/compute_note_duration.wgsl` (new)
- `public/shaders/compute_note_duration.wgsl` (copy)
- `utils/computeNoteDuration.ts` (new)
- `utils/gpuPacking.ts` — added `packPatternMatrixComputeInput` and `verifyDurationParity`
- `hooks/useWebGPURender.ts` — integrated compute path with fallback, dev parity check

**Key decisions:**
- `packPatternMatrixComputeInput` extracts cell fields identically to `packPatternMatrixHighPrecision` but packs into standard `[note][inst][volCmd][volVal]` / `[unused:16][effCmd][effVal]` format — this is the compute input.
- Compute shader does per-channel forward scan (1 thread per channel, `@workgroup_size(1,1,1)`), sequential row loops. Max rows = 1024 (IT format limit).
- Duration clamping to 255 rows replicated in all 3 scan branches (note-on end, note-off end, end-of-pattern tail).
- DURA-003 note copying, expression-only flag, volume nibble compression all replicated.
- Parity check runs async in dev mode: readback compute output → compare byte-for-byte with `packPatternMatrixHighPrecision` CPU output.

**Status:**
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, 16.5s)

### 2026-05-30 05:30+02:00 — Committed
Commit `a4d8978` with 6 files changed, 800 insertions, 35 deletions.

### Next steps / blockers
- **Browser smoke-test required** with at least one MOD and one IT module to verify the dev-mode parity assertion `[DURA-PARITY] ✓` prints in the console.
- If parity fails, the assertion logs the exact cell index and hex values of the first mismatch for debugging.
- No push/PR per instructions — Noah/Jules handle integration.

**Blockers:** None (implementation complete; pending runtime verification).

---

## MOBILE-LITE Render Mode — Implementation Log

### 2026-06-05 23:45+02:00 — Design & first pass
**Objective:** Implement a parallel "lite" render branch for mobile / low-power GPUs. Desktop path must remain byte-identical.

**Files touched:**
- `utils/deviceCapabilities.ts` (new)
- `utils/geometryConstants.ts`
- `utils/shaderRegistry.ts`
- `utils/shaderVersion.ts`
- `hooks/useWebGPURender.ts`
- `components/PatternDisplay.tsx`
- `App.tsx`

**Key decisions:**
- Capability detection is conservative: mobile UA string, missing WebGPU, and optional async low-power GPU adapter hint. URL param `?lite=1|0` and localStorage `xasm1_lite_mode` override auto-detection for desktop testing.
- Centralized policy object `DEVICE_CAPABILITIES` exported from `deviceCapabilities.ts`; consumers read flags, they don't re-detect.
- `patternv0.21.wgsl` tagged as `liteRecommended` in `shaderRegistry.ts` — horizontal layout, single-pass composite, no alpha blending, no layered bloom, supports 32-step paging.
- Lite mode auto-substitutes the display shader via `displayShaderFile` in `App.tsx` without mutating the user's stored desktop preference. Toggling lite off restores the previous shader instantly.
- Bloom initialization is skipped entirely in lite mode (`PatternDisplay.tsx` effect returns early when `liteMode` is true).
- WebGL overlay is disabled in lite mode (`isOverlayActive = !liteMode && WEBGL_HYBRID_SHADERS.has(...)`), saving an extra context.
- Canvas metrics reduced to 512×512 in lite mode (half the desktop 1024×1024), cutting fill-rate and memory.
- Compute path gated by `liteMode` in `useWebGPURender.ts` — falls back to CPU packing on the unlikely event a high-precision shader is forced in lite mode.
- DPR capped to 1.0 in lite mode (`PatternDisplay.tsx` `syncCanvasSize`), cutting physical canvas buffer size by up to 4× on high-DPR mobile screens.
- Desktop path is untouched: when `liteMode` is false, every code branch is identical to pre-change behavior (verified by conditional checks).

**Status:**
- `npm install` ✅ (missing `@tanstack/react-query` and `zod` installed)
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, 18.3s)

### Remaining / blockers
- **Runtime browser verification pending.** Need to confirm:
  1. `?lite=1` forces lite mode, substitutes v0.21, skips bloom, disables overlay, renders at 512×512.
  2. `?lite=0` forces full desktop mode even on mobile UA.
  3. Desktop default (`?lite` absent, localStorage empty, desktop UA) renders exactly as before (same shader, same bloom, same canvas size).
  4. No WebGPU console errors in either path.
- If runtime testing reveals any pipeline/buffer mismatch, the fix will be in `PatternDisplay.tsx` or `useWebGPURender.ts` (no structural changes expected).

**Blockers:** None (implementation complete; pending runtime verification).
## Parser → Renderer Pipeline Hardening — Implementation Log

### 2026-06-19 18:02+02:00 — Plan approved
**Objective:** Make the parser-worker → React state → renderer pipeline provably robust and self-verifying; guarantee visualization on WebGPU/WebGL2/HTML and graceful WebGPU-adapter-null fallback.

### 2026-06-19 19:15+02:00 — Implementation complete
**Files touched:**
- `utils/parserPromise.ts` (new)
- `utils/parserWorker.ts`
- `workers/openmpt-parser.worker.ts`
- `hooks/useLibOpenMPT.ts`
- `hooks/useWebGPURender.ts`
- `src/renderers/rendererSelection.ts`
- `components/PatternDisplay.tsx`
- `types.ts`
- `utils/__debug__/parserPromise.test.cjs` (new)

**Key decisions:**
- Extracted a testable `createParserPromise` helper in `utils/parserPromise.ts` that owns message/error/messageerror/timeout listener lifecycle and terminates the worker on timeout.
- `parserWorker.ts` now delegates to `createParserPromise`; `parseInWorker` no longer leaks listeners on any path.
- Worker now posts a `'fetch'` progress stage and aborts the CDN libopenmpt fetch after 12 s, so a stalled CDN request fails before the outer 15 s parser timeout.
- `useLibOpenMPT.ts` no longer gates the worker on the HEAD check (Vite hashes worker chunks in production, so the source `.ts` URL may 404 even though the emitted chunk is valid). The real availability test is `new Worker()` + error handling.
- Worker creation is wrapped in `try/catch`; constructor errors (404, CSP, MIME) immediately mark the worker unhealthy and fall through to main-thread parse.
- `resolveParsedModule` terminates and discards the worker in a `finally` block after any worker failure/timeout, guaranteeing the next load gets a fresh worker.
- Status machine now shows `(fetching parser engine)` / `(reading patterns)` progress and falls back silently to main-thread parse when the worker path fails.
- WebGPU fallback logs a clear `[Renderer] WebGPU unavailable (<reason>); falling back to <backend>` line exactly once per session; `PatternDisplay.tsx` guards against re-applying the fallback.
- Added `hasWebGPUAutoFallbackApplied()` helper for the session guard.
- `?renderer=webgpu` still attempts WebGPU first; fallback only on real init failure.

**Status (acceptance criteria 1–5):**
1. Default module shows populated pattern data on `?renderer=webgpu` (falls back to webgl2 in this headless env), `?renderer=webgl2`, and `?renderer=html` ✅
2. Worker 404/throw/no-response reaches `Error: …` within timeout and falls back to main-thread parse where CDN/libopenmpt is available ✅ (verified via Playwright route interception + main-thread fallback path)
3. WebGPU adapter null → auto webgl2 once per session, clear log, no hard overlay, no infinite loop ✅
4. `parseInWorker` cleans up listeners on every path ✅ (verified by unit test)
5. Explicit `?renderer=webgpu` attempts WebGPU first ✅

**Build / test results:**
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, ~17 s)
- `node utils/__debug__/parserPromise.test.cjs` ✅ (17 passed, 0 failed)
- Headless smoke via Playwright against `npm run preview` ✅ (html/webgl2/webgpu-with-fallback all load the default module)

**Next steps / blockers:**
- Runtime verification on a real WebGPU-capable browser (Chrome/Edge/Arc) to confirm WebGPU path renders populated pattern data, not just fallback.
- Optional: add a parser-worker route-interception smoke test in CI that asserts `[processModuleData]` + `[Parser] main-thread parse OK` logs when the worker chunk is blocked.
- No push/PR per instructions — Noah/Jules handle integration.

**Blockers:** None.

---

## Volume/Velocity-Reactive LED Layer (VEL-001) — Implementation Log

### 2026-06-26 16:40+00:00 — Plan approved & implementation
**Objective:** Add per-step volume/velocity reactivity to the three-emitter LED visualizer as a new `patternv0.57.wgsl` variant.

**Files touched:**
- `shaders/patternv0.57.wgsl` (new, copied from `patternv0.50.wgsl`)
- `public/shaders/patternv0.57.wgsl` (auto-synced)
- `utils/shaderRegistry.ts` — registered v0.57 with `bloomProfile: 'three-emitter'`
- `appConfig.ts` — added v0.57 to `SHADER_GROUPS.CIRCULAR`
- `utils/shaderVersion.ts` — added `'v0.57'` to `HIGH_PRECISION_SHADER_MARKERS`
- `components/PatternDisplay.tsx` — added `v0.57` to `padTopChannel` and `canvasMetrics` chains
- `hooks/useWebGPURender.ts` — added `v0.57` to `isHighPrec`, `effectiveCellW`, `playheadRowAsFloat`, and debug `isOverlayActive` chains

**Key decisions:**
- No new uniform added. Volume is decoded from the existing packed cell bytes:
  - `packedA` bits `[7:0]` hold the compressed `volCmd/volVal` nibble.
  - `packedB` bits `[7:0]` hold the full `volCmd` byte.
  - `normalizedCellVolume()` returns `1.0` when no volume column is present (full velocity default), otherwise `clamp(coarseVol / 4.0, 0.0, 1.0)`.
- Bottom emitter repurposed from amber expression indicator to a cyan velocity/volume indicator for pitched notes only (`isNoteOn || isSustain`).
- Top and middle emitter intensities are scaled by `(0.65 + 0.45 * volumeFactor)` for note-ons and `(0.75 + 0.25 * volumeFactor)` for sustain tails, keeping changes pre-ACES.
- Sustain tails receive a subtle organic pulse: `1.0 + 0.07 * sin(timeSec * 3.0 + row * 0.4)`.
- Expression-only, note-off, empty, and dead-cell paths are unchanged; `octaveBrightness` is untouched.
- `patternv0.50.wgsl` and `patternv0.56.wgsl` remain byte-identical; the packing contract was not modified.

**Build / test results:**
- `npm run sync:shaders` ✅
- `npm run typecheck` ✅ (zero errors)
- `npm run build` ✅ (4 GB heap, ~17.5 s)
- `npm run test:octave-brightness` ✅
- `npm run test:trigger-tail` ✅
- `npm run test:duration-parity` ✅
- `npm run test:shader-registry` ✅

**Next steps / blockers:**
- Runtime browser verification on a WebGPU-capable browser with a dynamics-rich module to confirm:
  1. Loud steps glow brighter and bloom harder than quiet ones.
  2. Bottom emitter reads as a velocity indicator.
  3. Sustain tails show an organic brightness pulse.
  4. No `[WebGPU]` pipeline-creation errors when `patternv0.57.wgsl` is selected.
- No push/PR per instructions — Jules handles integration.

**Blockers:** None.

---

## Night Shader Trio (v0.52/0.53/0.54) — Implementation Log

### 2026-07-04 03:30+02:00 — v0.52 Night, v0.53 Midnight, v0.54 Neon Night implemented
**Objective:** Create three distinct dark-theme circular WGSL pattern shaders for the XASM-1 browser tracker visualizer, combining v0.51's playhead-arc scan line with v0.56's per-instrument palette texture sampling.

**Files touched:**
- `shaders/patternv0.52.wgsl` (new, "Night")
- `shaders/patternv0.53.wgsl` (new, "Midnight")
- `shaders/patternv0.54.wgsl` (new, "Neon Night")
- `public/shaders/patternv0.52.wgsl` (auto-synced)
- `public/shaders/patternv0.53.wgsl` (auto-synced)
- `public/shaders/patternv0.54.wgsl` (auto-synced)
- `utils/shaderRegistry.ts` — registered all three with `bloomProfile: 'three-emitter'`
- `appConfig.ts` — added v0.52/0.53/0.54 to `SHADER_GROUPS.CIRCULAR`
- `utils/shaderVersion.ts` — added `'v0.52'`, `'v0.53'`, `'v0.54'` to `HIGH_PRECISION_SHADER_MARKERS` and `usesPlayheadRowAsFloat`
- `components/PatternDisplay.tsx` — added the three versions to `padTopChannel` and `canvasMetrics` chains
- `hooks/useWebGPURender.ts` — added the three versions to instrument-palette bind-group layout, initial texture creation, per-frame palette upload, `effectiveCellW`, and debug `isOverlayActive` chains

**Key theming decisions (all via in-shader constants, no new uniforms):**
- **v0.52 "Night"** — dim dusky night:
  - `THEME_BG = (0.025, 0.027, 0.032)`, `THEME_LED_OFF = (0.035, 0.037, 0.045)`
  - `THEME_LED_ON = (0.95, 0.50, 0.08)` warm amber
  - `THEME_RIM = (0.20, 0.30, 0.45)` cool blue steel
  - `THEME_ARC = (0.85, 0.75, 0.30)` muted gold
  - `THEME_BLOOM_MULT = 0.90` (slightly subdued bloom)
- **v0.53 "Midnight"** — near-black deep midnight, highest contrast:
  - `THEME_BG = (0.008, 0.008, 0.010)`, `THEME_LED_OFF = (0.012, 0.012, 0.015)`
  - `THEME_LED_ON = (1.00, 0.65, 0.15)` bright amber
  - `THEME_RIM = (0.10, 0.12, 0.18)` dark steel
  - `THEME_ARC = (1.00, 0.85, 0.35)` bright gold
  - `THEME_BLOOM_MULT = 1.00`
- **v0.54 "Neon Night"** — saturated neon-noir, brightest bloom:
  - `THEME_BG = (0.015, 0.010, 0.020)` deep purple-black
  - `THEME_LED_OFF = (0.020, 0.015, 0.025)`
  - `THEME_LED_ON = (0.00, 0.90, 1.00)` electric cyan
  - `THEME_RIM = (0.00, 0.90, 1.00)` electric cyan rim glow
  - `THEME_ARC = (1.00, 0.00, 0.80)` magenta playhead arc
  - `THEME_KICK = (0.00, 0.85, 1.00)` cyan kick reactive glow
  - `THEME_BLOOM_MULT = 1.15` (brightest bloom of the trio)

**Shader architecture:**
- Uniform struct matches `v0.56` exactly (132-byte extended layout, `paletteMode` at slot 32, `instrumentPalette` at `@binding(7)`).
- Playhead-arc overlay copied verbatim from `v0.51` (ARC-001), including `discard` of the bottom 12% UI band.
- Per-instrument palette sampling path copied from `v0.56`: when `paletteMode == 1u`, middle emitter reads `textureLoad(instrumentPalette, inst % 64, 0)`.
- Three-emitter LED logic, DURA duration tails, and ACES tonemapping remain unchanged from the v0.50/v0.51 baseline.

**Build / test results:**
- `npm run sync:shaders` ✅
- `npm run typecheck` ✅ (zero errors)
- `npm run test:shader-registry` ✅ (28 shaders, 3 bloom profiles)
- `npm run test:octave-brightness` ✅
- `npm run test:trigger-tail` ✅
- `npm run test:duration-parity` ✅
- `npm run build` ✅ (4 GB heap, ~17 s)

**Git state note:**
- Working-tree source files and host-side edits were already captured in a local `push fix` commit (`6f2a984`) before this log was written.
- `git diff` is clean; no unintended existing files were modified.
- `public/shaders/patternv0.53.wgsl` and `public/shaders/patternv0.54.wgsl` are present as auto-synced copies (v0.52 public copy is already tracked).

**Next steps / blockers:**
- Runtime browser verification on a WebGPU-capable browser to confirm:
  1. All three shaders appear in the shader picker and switch cleanly.
  2. Playhead arc is visible on all three.
  3. Per-instrument palette mode works when enabled.
  4. No `[WebGPU]` pipeline-creation or WGSL parse errors for v0.52/0.53/0.54.
- No push/PR per instructions — Jules integrates at end of day.

**Blockers:** None.
